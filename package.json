{
  "name": "haus-des-kiosks-website",
  "private": true,
  "type": "module",
  "scripts": {
    "check:js": "node --check site-config.js && node --check script.js",
    "test:static": "node scripts/static-check.mjs",
    "test:unit": "node --test tests/unit/*.test.mjs",
    "test": "npm run check:js && npm run test:static && npm run test:unit"
  },
  "engines": {
    "node": ">=22"
  }
}
